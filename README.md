# ESG Reporting Helper

This simple Flask app provides a minimal interface for entering company details and generating a draft double materiality matrix. It demonstrates how to start organizing information for CSRD reporting.

## Setup

Install dependencies and run the server:

```bash
pip install -r requirements.txt
python app.py
```

Open `http://localhost:5000` in your browser to access the form.
